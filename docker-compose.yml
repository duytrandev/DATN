version: "3.8"

services:
  api-predict:
    build: 
      context: ./fast-api
      dockerfile: Dockerfile
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
        - ./:/app
    restart: always
